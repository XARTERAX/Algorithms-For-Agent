---
title: "Short English Title"
id: 0000
source: ""
difficulty: "Easy/Medium/Hard"
topics: ["agent", "graph"]
languages: ["python"]
time_complexity: "O(?)"
space_complexity: "O(?)"
date: YYYY-MM-DD
---

# Problem

(Problem statement in English, concise. Describe input format, output format, and constraints.)

# Examples

**Example 1:**
- Input: `...`
- Output: `...`
- Explanation: ...

**Example 2 (edge case):**
- Input: `...`
- Output: `...`

# Brute Force

Describe the naive solution:
- Approach: ...
- Why it is correct: ...
- Time complexity: O(?)
- Space complexity: O(?)

# Optimized Idea

Thought chain:
1. **Model the problem** — represent as graph / grid / state space.
2. **Identify the bottleneck** — what makes brute force slow?
3. **Pattern** — sliding window / BFS / DP / greedy / A* / minimax ...
4. **Key invariant or transition** — state what property is maintained.
5. **Complexity improvement** — from O(?) to O(?).

# Pseudocode

```
function solve(input):
    // step 1
    // step 2
    return result
```

# Implementation

```python
# See implementations/python/0000_short_title.py
```

# Tests

```python
# See tests/test_0000_short_title.py
assert solve(...) == ...
assert solve(...) == ...
```

# Notes

> **Key idea:** one-sentence summary of the core trick.  
> **Complexity:** Time O(?), Space O(?).  
> **Next improvements:** what could be optimized or extended next.
